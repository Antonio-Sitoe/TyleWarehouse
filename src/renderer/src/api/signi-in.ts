export function authenticateWithEmail({ password, email }: { email: string; password: string }) {
  console.log({ password, email })
}
export function authenticateWithPhone({ password, phone }: { phone: string; password: string }) {
  console.log({ password, phone })
}
